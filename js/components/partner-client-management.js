function addNewClient() {
  alert("새 클라이언트 추가 기능은 준비 중입니다.");
}

function viewPartnership(clientId) {
  alert(`클라이언트 ${clientId}의 파트너십을 조회합니다.`);
}

function manageClient(clientId) {
  alert(`클라이언트 ${clientId} 관리 페이지로 이동합니다.`);
}

function initPartnerClientManagement() {
  // 필요 시 이벤트 추가 바인딩
}

window.initPartnerClientManagement = initPartnerClientManagement;
